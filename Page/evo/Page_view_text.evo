# inclusion to display text
backlinks=self.get_backlinks(req,formatted=True)
if self.get_images():
  div:cls='col-8'
    self.text.formatted(req)
    if backlinks:
        hr:
        backlinks
  div:cls='col-4'
    self.Page_view_images.evo
else:
  div:cls='col-12'
    self.text.formatted(req)
    if backlinks:
        hr:
        backlinks
